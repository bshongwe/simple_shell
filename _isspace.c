#include "ctype.h"

/**
 * _isspace - func verifies if char is whitespace
 * @c: char input
 * Return: char is whitespace (1), otherwise (0)
 */
bool _isspace(int c)
{
	return (c == ' ' || (c >= 0x09 && c <= 0x0d));
}#include "ctype.h"

/**
 * _isspace - func verifies if char is whitespace
 * @c: char input
 * Return: char is whitespace (1), otherwise (0)
 */
bool _isspace(int c)
{
	return (c == ' ' || (c >= 0x09 && c <= 0x0d));
}#include "ctype.h"

/**
 * _isspace - func verifies if char is whitespace
 * @c: char input
 * Return: char is whitespace (1), otherwise (0)
 */
bool _isspace(int c)
{
	return (c == ' ' || (c >= 0x09 && c <= 0x0d));
}
